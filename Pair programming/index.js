let variableSinValor;

let booleano1=true;
let booleano2=false;

const PI= 3.14;
const TAU= 2 * 3.14;

let booleanoAnd = booleano1 && booleano2;

let booleanoNot = !booleano1;

let booleanoMixO = () {
    for ((booleano1 || booleano2) && (booleano1 || (!booleano1 & !booleano2)))
}
 
let incrementarDesp = 2 ;
let resultadoDesp = incrementarDesp++;

let incrementarAntes = 2 ;
let resultadoAntes = incrementarAntes--;

let contarHasta10_2 = 0 ;
for (contarHasta10_2; contarHasta10_2++; contarHasta10_2==10);

let postl = 0;
let postJ = 0;
let i = 0  ; 
while (i<10 ) {
    postl = postl + postJ++;
    i++
}


SumaPares = 0;
for (i = 0; i < 10; i++) {
    if (i / 2 == 0) {
        (SumaPares = SumaPares + i);
    }
}

