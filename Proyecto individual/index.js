let variableValorNumerico= 8;

const MiNombre= "Gema";

const MiNumeroFavorito = 7;

let booleanoOr = (booleano1 || booleano2);

let booleanoMix1 = (booleano1 && (TAU / 2== 3.14)||(variableValorNumerico >= MinumeroFavorito));

let seisNoEsNueve = (6=9);

let booleanoMix2 =( variableValorNumerico+(-0) || ( MiNumeroFa * TAU));

let valorSuma= (miNumeroFav + variableValorNumerico);

let valorResta= (miNumeroFavorito - variableValorNumerico);

let valorMultiplicacion = MiNumeroFav* variableValorNumerico;

let valorDivision= MiNumeroFavorito/ 3;

let contarhasta10= (0);
    while (contarHasta10==10) {
        contarHasta10++;
    }

let preI= 0;
let preJ= 0;
    for (let i = 0; i < 11; i++) {
    preI += ++preJ;
}


sumalmpares = 0;
    while (i < 10) {
     if (sumaPares%1){
        sumaPares+ i;
     }
}




